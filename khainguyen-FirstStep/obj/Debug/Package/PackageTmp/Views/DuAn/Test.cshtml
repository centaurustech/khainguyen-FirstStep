@{
    ViewBag.Title = "Tổng hợp thông tin Crowdfunding";
}

@section MeTaHeader
{
    <meta name="robots" content="noindex,nofollow" />
}

 <!-- SLIDE -->
<div class="slider-fix-300">
<div id="slides-sm">
    <ul class="slides-container">
        <li><img src="~/Content/cssFrontEnd/upload/bigslider/img7.jpg" /></li>
        <li><img src="~/Content/cssFrontEnd/upload/bigslider/img8.jpg" /></li>>
    </ul>

    <div class="faq-intro">
        <div class="container">
            <div class="faq-logo"><i class="fa-support fa-spin"></i></div>
            <div class="faq-info">
                <div class="faq-title">Tổng hợp thông tin Crowdfunding</div>
                <div class="faq-des"></div>
            </div>
        </div>
    </div>
</div>
    </div>
@* <!-- SLIDE -->
        <div class="slider-fix-300">
            <div id="slides-sm">
                <ul class="slides-container">
                     <li><img src="~/Content/cssFrontEnd/upload/bigslider/img7.jpg" /></li>
        <li><img src="~/Content/cssFrontEnd/upload/bigslider/img8.jpg" /></li>>
                </ul>
                <div class="faq-intro">
                    <div class="container">
                        <div class="faq-logo"><i class="fa-support fa-spin"></i></div>
                        <div class="faq-info">
                            <div class="faq-title">FAQ</div>
                            <div class="faq-des faq-gray">Looking for inspiration, tips, tools, and best practices for making a successful project?</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>*@


<!-- FAQ -->
<div class="container">

    @*<div class="faq-search">
        <input class="form-txt" type="text" placeholder="search anything, you need help :)" /><i class="fa-search"></i>
    </div>*@   
    <label>Username, Phone, Or Email</label><input id="thongtin" name="thongtin" type="text" >
    <button onclick="return duan()">Kiểm tra</button>


</div>

<div style="height: 50px;"></div>
@section jqueryPlugin{
<script>
    $(document).ready(function () {
        var $slides = $('#slides-sm');
        Hammer($slides[0]).on("swipeleft", function (e) {
            $slides.data('superslides').animate('next');
        });

        Hammer($slides[0]).on("swiperight", function (e) {
            $slides.data('superslides').animate('prev');
        });
        $slides.superslides({
            slide_easing: 'easeInOutCubic',
            slide_speed: 800,
            pagination: false,
            hashchange: false,
            scrollable: true,
            play: 5000,
            animation: 'fade',
            inherit_height_from: '.slider-fix-300'
        });
        group(2);
    });
    $('.faq-ans').each(function () {
        $(this).attr('data-height', $(this).height()).height(0);
    });
    $('.faq-ask a:first-child').click(function (e) {
        e.preventDefault();
        if ($(this).attr('data-show') == "1") {
            $(this).attr('data-show', 0);
            $(this).next().animate({ height: 0 }, 500);
        }
        else {
            $(this).attr('data-show', 1);
            $n = $(this).next();
            $n.animate({ height: $n.attr('data-height') }, 500);
            //$('.faq-ans').animate({ height: 0 }, 300, function () {
            //    $('.faq-ask a:first-child').attr('data-show', 0);
            //    $(this).attr('data-show', 1);
            //    $n.animate({ height: $n.attr('data-height') }, 500);
            //});
        }
    });
    @*    function group(id) {
        var postURL = '@Url.Action("_IndexFAQ1_NoiDung", "FAQ")';
        $.post(postURL, { "id": id }, function (data) {
            if (data == "Error") {
                alert("Error");
            } else {
                $('#noidung').html(data);
            }
        });
        return false;
    }*@
    //$(document).ready(function () {
    //    var $slides = $('#slides');
    //    Hammer($slides[0]).on("swipeleft", function (e) {
    //        $slides.data('superslides').animate('next');
    //    });

    //    Hammer($slides[0]).on("swiperight", function (e) {
    //        $slides.data('superslides').animate('prev');
    //    });
    //    $slides.superslides({
    //        slide_easing: 'easeInOutCubic',
    //        slide_speed: 800,
    //        pagination: false,
    //        hashchange: false,
    //        scrollable: true,
    //        play: 5000,
    //        animation: 'fade'
    //    });
    //});     
    $(document).ready(function () {        
    });
    function duan() {
        var username = $('#thongtin').val();
        var postURL = '@Url.Action("CheckPayooAccount", "Duan")';
        $.post(postURL, { "username": username }, function (data) {
            alert(data);
        });
        return false;
    }
</script>

}